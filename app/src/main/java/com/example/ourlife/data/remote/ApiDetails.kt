package com.example.ourlife.data.remote

object ApiDetails {

    const val BASE_URL = "https://jsonplaceholder.typicode.com/"

    const val USER_PROFILE = "users/{id}"

    const val USER_POSTS = "users/{id}/posts"

    const val USER_ALBUMS = "users/{id}/albums"

    const val USER_TODOS = "users/{id}/todos"

    const val POST_COMMENTS = "posts/{id}/comments"

}